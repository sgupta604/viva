---
description: Show pipeline status — current feature, phase, next command
---

Read `.claude/pipeline/STATUS.md` and report:

```
Feature: [name or "none"]
Phase: [current phase]
Next: [/command]
Branch: [branch]
```

If there are queued or parked features, list them briefly.

Cross-check: verify the files that STATUS.md claims exist actually do exist. If there's a discrepancy, report it.

Do NOT spawn an agent. Do NOT modify files. Read and report.
